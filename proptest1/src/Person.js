function Person({person}) {
    return (
        <div>
            <h2>{person.name}</h2>
            <h2>{person.age}</h2>
            <h2>{person.nick}</h2>
        </div>
    )
}

export default Person;